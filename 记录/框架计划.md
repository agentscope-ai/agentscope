根据你的前端功能需求，逐一映射到 QwenPaw 功能模块，并标注该模块在 AgentScope 还是 QwenPaw 中实现：

---

## 前端功能 → QwenPaw 功能模块映射

### 一、对话核心链路（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 | 说明 |
|---------|-------------|--------|------|
| 发送 threadId | **8阶段请求编排** + **SSE事件信封** | QwenPaw `runtime/` | 核心对话入口，接收 threadId 启动 Runtime |
| 对话接口，SSE流解析 | **SSE事件信封** | QwenPaw `runtime/envelope.py` | 已内置 SSE 状态机，前端直接解析 event-stream |
| 停止对话 | **心跳执行器** + **Agent执行器** | QwenPaw `runtime/executor.py` | 需实现取消信号传播到 Agent reply 流 |

> **你需要实现的核心模块：**
> - [x] `runtime/runtime.py` — 8阶段编排
> - [x] `runtime/envelope.py` — SSE 信封
> - [x] `runtime/executor.py` — 执行器（含停止信号）
> - [x] `runtime/builder.py` — Agent 组装器
> - [x] `agents/react_agent.py` — QwenPawAgent

---

### 二、会话/线程管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取线程列表 | **聊天会话管理** | QwenPaw `app/chats/` |
| 获取线程历史对话 | **聊天会话管理** | QwenPaw `app/chats/repo/` |
| 修改对话标题 | **聊天会话管理** | QwenPaw `app/chats/` |
| 删除历史对话记录 | **聊天会话管理** | QwenPaw `app/chats/` |

> **你需要实现：**
> - [x] `app/chats/` — 会话 CRUD + 消息存储
> - [x] `app/routers/agent_scoped.py` — Agent 级路由（chats）

---

### 三、模型管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取模型列表 | **多模型路由** + **Provider管理** | QwenPaw `providers/` |

> **你需要实现：**
> - [x] `providers/provider_manager.py` — 模型列表查询
> - [x] `app/routers/providers.py` — Provider API

---

### 四、文件管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 上传文件 | **文件I/O** + **工作空间** | QwenPaw `app/routers/files.py` |

> **你需要实现：**
> - [x] `app/routers/files.py` — 文件上传/下载路由

---

### 五、智能体管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 查询智能体列表 | **多Agent管理** | QwenPaw `app/multi_agent_manager.py` |
| 创建智能体 | **多Agent管理** + **配置管理** | QwenPaw `app/multi_agent_manager.py` |
| 删除智能体 | **多Agent管理** | QwenPaw `app/multi_agent_manager.py` |
| 修改智能体 | **多Agent管理** + **配置热监听** | QwenPaw `app/multi_agent_manager.py` + `agent_config_watcher.py` |

> **你需要实现：**
> - [x] `app/multi_agent_manager.py` — 多 Agent 生命周期
> - [x] `app/workspace_registry.py` — 工作空间注册
> - [x] `app/routers/agents.py` — Agent 管理 API
> - [x] `config/` — Agent 配置文件读写

---

### 六、子智能体管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取所有子智能体列表 | **Agent管理工具** | QwenPaw `agents/tools/agent_management.py` |
| 创建子智能体 | **Agent管理工具** | QwenPaw `agents/tools/agent_management.py` |
| 删除子智能体 | **Agent管理工具** | QwenPaw `agents/tools/agent_management.py` |
| 修改子智能体 | **Agent管理工具** | QwenPaw `agents/tools/agent_management.py` |

> **你需要实现：**
> - [x] `agents/tools/agent_management.py` — 子 Agent CRUD
> - [x] `app/routers/agents.py` — 扩展子 Agent 路由

---

### 七、转交配置（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取转交配置 | **外部Agent委派** | QwenPaw `agents/tools/delegate_external_agent.py` |
| 修改转交配置 | **外部Agent委派** | QwenPaw `agents/tools/delegate_external_agent.py` |

> **你需要实现：**
> - [x] `agents/tools/delegate_external_agent.py` — 外部 Agent 委派配置
> - [x] `app/routers/agent_scoped.py` — 扩展转交配置路由

---

### 八、工具管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取工具列表 | **工具注册表** | QwenPaw `runtime/tool_registry.py` |
| 添加工具 | **工具注册表** + **Toolkit** | QwenPaw `runtime/tool_registry.py` + AgentScope `tool/toolkit.py` |
| 取消添加工具 | **工具注册表** | QwenPaw `runtime/tool_registry.py` |

> **你需要实现：**
> - [x] `runtime/tool_registry.py` — 工具增删查
> - [x] `app/routers/tools.py` — 工具管理 API

---

### 九、技能管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取已安装技能 | **技能系统** | QwenPaw `agents/skill_system/` |
| 获取所有技能列表 | **技能系统** + **技能池** | QwenPaw `agents/skill_system/` |
| 查询个人上传技能列表 | **技能Hub** | QwenPaw `agents/skill_system/hub.py` |
| 添加技能 | **技能系统** | QwenPaw `agents/skill_system/` |
| 取消添加技能 | **技能系统** | QwenPaw `agents/skill_system/` |

> **你需要实现：**
> - [x] `agents/skill_system/` — 技能注册/加载/增删
> - [x] `app/routers/skills.py` — 技能管理 API
> - [x] `app/routers/skills_stream.py` — 技能流式 API

---

### 十、记忆系统（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取记忆内容 | **长期记忆** + **记忆中间件** | QwenPaw `agents/memory/` + `agents/middlewares.py` |

> **你需要实现：**
> - [x] `agents/memory/` — 记忆存储与查询
> - [x] `agents/middlewares.py` — MemoryMiddleware
> - [x] `app/routers/` — 扩展记忆查询路由

---

### 十一、Token用量（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取线程消耗的token数 | **Token用量管理** | QwenPaw `token_usage/` |

> **你需要实现：**
> - [x] `token_usage/manager.py` — 用量管理器
> - [x] `token_usage/storage.py` — 持久化存储
> - [x] `app/routers/token_usage.py` — 用量查询 API（按 thread 维度）

---

### 十二、AI产物管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取AI产物列表 | **检查点系统** 或 **工具产出管理** | QwenPaw `checkpoints/` 或 `app/routers/` |
| 下载AI产物文件 | **文件I/O** | QwenPaw `app/routers/files.py` |

> **你需要实现：**
> - [x] `app/routers/checkpoints.py` — 产物/检查点列表
> - [x] `app/routers/files.py` — 文件下载

---

### 十三、知识库管理（必须实现）

| 前端功能 | 依赖的功能模块 | 实现层 |
|---------|-------------|--------|
| 获取知识库列表 | **RAG/知识库** | AgentScope `rag/` + `app/_service/_knowledge_base.py` |
| 创建知识库 | **RAG/知识库** | AgentScope `rag/` + QwenPaw `app/routers/` |
| 更新知识库 | **RAG/知识库** | AgentScope `rag/` |
| 删除知识库 | **RAG/知识库** | AgentScope `rag/` |

> **你需要实现：**
> - [x] AgentScope `rag/` — 知识库核心（已有基类）
> - [x] AgentScope `app/_router/_knowledge_base.py` — 知识库路由（已有）
> - [x] QwenPaw `app/routers/` — 扩展知识库业务路由

---

## 总结：你需要实现的 QwenPaw 功能模块

按优先级排序：

### P0 — 核心链路（先做这些，否则前端无法跑通）

| # | 功能模块 | 对应前端功能 |
|---|---------|------------|
| 1 | **8阶段请求编排** (`runtime/`) | 对话接口+SSE流 |
| 2 | **SSE事件信封** (`runtime/envelope.py`) | SSE流解析 |
| 3 | **Agent执行器** (`runtime/executor.py`) | 停止对话 |
| 4 | **Agent组装器** (`runtime/builder.py`) | 对话接口 |
| 5 | **QwenPawAgent** (`agents/react_agent.py`) | 对话接口 |
| 6 | **聊天会话管理** (`app/chats/`) | 线程列表/历史/标题/删除 |
| 7 | **多模型路由** (`providers/`) | 获取模型列表 |
| 8 | **多Agent管理** (`app/multi_agent_manager.py`) | 智能体CRUD |

### P1 — 管理功能（核心跑通后做）

| # | 功能模块 | 对应前端功能 |
|---|---------|------------|
| 9 | **工具注册表** (`runtime/tool_registry.py`) | 工具增删查 |
| 10 | **技能系统** (`agents/skill_system/`) | 技能增删查 |
| 11 | **记忆系统** (`agents/memory/`) | 获取记忆内容 |
| 12 | **Token用量管理** (`token_usage/`) | 线程Token消耗 |
| 13 | **文件I/O** (`app/routers/files.py`) | 上传/下载文件 |
| 14 | **子Agent管理** (`agents/tools/agent_management.py`) | 子智能体CRUD |

### P2 — 扩展功能（管理功能完成后做）

| # | 功能模块 | 对应前端功能 |
|---|---------|------------|
| 15 | **外部Agent委派** (`agents/tools/delegate_external_agent.py`) | 转交配置 |
| 16 | **检查点/产物** (`checkpoints/` + `app/routers/checkpoints.py`) | AI产物列表/下载 |
| 17 | **知识库** (AgentScope `rag/` + QwenPaw 路由扩展) | 知识库CRUD |

### 不需要实现的 QwenPaw 模块（前端未涉及）

| 模块 | 原因 |
|------|------|
| ~19个渠道集成~ | 前端只有 Web Console，不需要钉钉/飞书等 |
| ~浏览器自动化~ | 前端无浏览器操作功能 |
| ~沙箱隔离~ | 前端无沙箱需求 |
| ~备份系统~ | 前端无备份功能 |
| ~插件市场~ | 前端无插件市场 |
| ~CLI/TUI~ | 前端是 Web 端 |
| ~Tauri桌面端~ | 前端是 Web 端 |
| ~Cloudflare隧道~ | 前端无内网穿透需求 |
| ~本地模型~ | 前端获取模型列表走 Provider 即可 |
| ~测试框架~ | 前端无测试需求 |
| ~定时任务~ | 前端无 Cron 功能 |
| ~审批流程~ | 前端无审批功能 |
| ~Langfuse可观测性~ | 前端无可观测性需求 |
| ~运行模式切换~ | 前端无模式切换 |
| ~生命周期Hook（大部分）~ | 按需启用 |

---

请确认这个映射是否符合你的预期，以及是否有优先级调整需求。确认后我可以开始按 P0 → P1 → P2 顺序帮你搭建框架。