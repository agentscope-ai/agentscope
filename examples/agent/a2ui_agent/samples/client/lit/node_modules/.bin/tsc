#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/../typescript/bin/tsc" ]; then
  "$basedir/../typescript/bin/tsc" "$@"
else
  node "$basedir/../typescript/bin/tsc" "$@"
fi
