"""Custom agent middlewares — put your product-specific middlewares here.

Create modules (e.g. ``rate_limit.py``, ``audit.py``) and register
them via ``MiddlewareRegistry.register()`` in ``main.py``.
"""
