# Contracts

This absorption batch does not introduce or change external API/tool schemas.

Scope is limited to:

- Internal plan state serialization/state-transition behavior.
- Internal Word reader import/type handling.
- Regression coverage in `tests/plan_test.py`.
