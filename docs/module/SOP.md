# SOP：src/agentscope/module 模块

## 一、功能定义（Scope）
- 层次：状态化模块基类与通用机制（StateModule）。

## 二、文件 / 类 / 函数 / 成员变量

### 文件：src/agentscope/module/_state_module.py
- 类：`StateModule`

## 三、与其他组件的交互关系
- Session：持久化 `StateModule` 的状态。

## 四、变更流程
同 AGENTS.md。
