# SOP：src/agentscope/embedding 模块

## 一、功能定义（Scope）
- 层次：嵌入生成与缓存层。

## 二、文件 / 类 / 函数 / 成员变量

### 文件：src/agentscope/embedding/_openai_embedding.py
- 类：`OpenAITextEmbedding`

### 文件：src/agentscope/embedding/_embedding_base.py
- 类：`EmbeddingModelBase`

（缓存/用量/响应类按需补全）

## 三、与其他组件的交互关系
- RAG：构建/查询向量。

## 四、变更流程
同 AGENTS.md。
