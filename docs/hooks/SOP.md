# SOP：src/agentscope/hooks 模块

## 一、功能定义（Scope）
- 层次：Studio/可视化相关的钩子装配。

## 二、文件 / 类 / 函数 / 成员变量

### 文件：src/agentscope/hooks/_studio_hooks.py
- 函数：`_equip_as_studio_hooks`

## 三、与其他组件的交互关系
- Agent：在 `agentscope.init(studio_url=...)` 时挂载运行时钩子。

## 四、变更流程
同 AGENTS.md。
